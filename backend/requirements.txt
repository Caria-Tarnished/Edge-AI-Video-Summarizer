fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
faster-whisper==1.0.3; python_version < '3.13'
huggingface_hub>=0.20.0
imageio-ffmpeg==0.5.1
dashscope>=1.14.0
chromadb>=0.5.0
fastembed>=0.3.0; python_version < '3.13'
# PyInstaller 6.3.0 与 numpy 2.x 存在模块加载冲突（cannot load module more than once per process），
# 固定到 numpy 1.x 兼容范围以确保打包后 faster-whisper 等 C 扩展能正常加载。
numpy>=1.24.0,<2.0
